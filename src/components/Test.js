import React from 'react'

function Test(props) {
    // const login=(props.data.login_details);
    return (
        <div>
            <h1>{JSON.stringify(props)}</h1>
        </div>
    )
}

export default Test
